node_modules/
dist/
.DS_Store
**/.DS_Store
*.local
.vite/

# Environment variables — never commit secrets
.env
.env.*
!.env.example

# Claude Code — local permissions cache (machine-specific, not for version control)
.claude/settings.local.json
# Claude Code — isolated agent worktrees (local-only, not for version control)
.claude/worktrees/

# Superpowers — isolated worktrees and brainstorm/scratch state
.worktrees/
.superpowers/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Local project log — running summary, not for version control
PROJECT_LOG.md

# Local beta-readiness checklist — internal planning, not for version control
BETA_READINESS.md

# Local budgeting spreadsheets — not project code
krewtree-expense-table.xlsx
krewtree-monthly-budget.xlsx

# Supabase CLI — local scratch (linkage cache, version checks), not for version control
supabase/.temp/
supabase/.branches/
